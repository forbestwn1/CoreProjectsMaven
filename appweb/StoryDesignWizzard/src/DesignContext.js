import { createContext } from 'react';

export const DesignContext = createContext(null);
export const DesignDispatchContext = createContext(null);
export const DataSourceContext = createContext(null);
