var library = nosliw.getPackage("debug");

/*
 * component is abstract concept
 * 	   contains parts
 *     predefined lifecycle status (init, active, paused, destoryed)
 *     configurable
 *     have state to support backup, resume
 */
